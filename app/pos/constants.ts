// Reserved category id (auto-slugified from the "Add ons" label) whose items
// are sourced as the order panel's attachable add-ons, instead of being
// orderable menu items in their own right.
export const ADDON_CATEGORY_ID = 'add-ons'
